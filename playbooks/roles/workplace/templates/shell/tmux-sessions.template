#!/usr/bin/env bash


{% for s in tmux_predefined_sessions %}
tmux has-session -t '{{ s }}' 2>/dev/null
if [ "$?" -eq 1 ] ; then
    tmux new-session -s '{{ s }}' -d 'mc'
fi
{% endfor %}

tmux attach -t {{ default_tmux_session }}
