fpath=(~/.zsh/function $fpath)

mkd(){ mkdir "$1" && cd "$1"; }
rmd(){ local P="`pwd`"; cd .. && rmdir "$P" || cd "$P"; }

dot() {
	if [[ $LBUFFER = *.. ]]; then
		LBUFFER+=/..
	else
		LBUFFER+=.
	fi
}
